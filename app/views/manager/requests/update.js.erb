$("#reject_btn").remove()
$("#approve_btn").remove()
<% if params[:request][:status_id].to_i == Settings.status.rejected %>
  $("#req_status_name").html($("#reject-label"))
  $("#rejectConfirmModal").modal('hide');
<% else %>
  $("#req_status_name").html($("#approve-label"))
  $("#approveConfirmModal").modal('hide');
<% end %>
